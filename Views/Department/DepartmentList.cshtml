@{
    ViewData["Title"] = "Department List";
}

<div class="pagetitle d-flex justify-content-between align-items-center mb-3">
    <h1 class="m-0">Department Table</h1>

    <a asp-action="DepartmentAddEdit" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Add
    </a>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <div class="mt-3 mb-3">
                        <input type="text" id="deptSearch" class="form-control" placeholder="Search Departments...">
                    </div>
                    <!-- Table with stripped rows -->
                    <table class="table table-striped" id="deptTable">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Department Name</th>
                                <th>Created</th>
                                <th>Modified</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (ViewBag.Departments != null)
                            {
                                @foreach (var department in ViewBag.Departments as List<MOM_Project.Models.DepartmentModel>)
                                {
                                    <tr>
                                        <td>@department.DepartmentID</td>
                                        <td>@department.DepartmentName</td>
                                        <td>@department.Created.ToShortDateString()</td>
                                        <td>@department.Modified.ToShortDateString()</td>
                                        <td>
                                            <a asp-action="DepartmentAddEdit" asp-route-id="@department.DepartmentID" class="btn btn-sm btn-primary">Edit</a>
                                            <a asp-action="Delete" asp-route-id="@department.DepartmentID" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?')">Delete</a>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    document.getElementById('deptSearch').addEventListener('keyup', function() {
        let value = this.value.toLowerCase();
        let rows = document.querySelectorAll('#deptTable tbody tr');
        rows.forEach(row => {
            row.style.display = row.innerText.toLowerCase().includes(value) ? '' : 'none';
        });
    });
</script>
