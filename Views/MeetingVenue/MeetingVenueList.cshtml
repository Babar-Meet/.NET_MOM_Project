@{
    ViewData["Title"] = "Meeting Venue List";
}

<div class="pagetitle d-flex justify-content-between align-items-center mb-3">
    <h1 class="m-0">Meeting Venue Table</h1>

    <a asp-controller="MeetingVenue"
       asp-action="MeetingVenueAddEdit"
       class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Add
    </a>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <!-- Table with stripped rows -->
                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th>Venue ID</th>
                                <th>Venue Name</th>
                                <th>Created</th>
                                <th>Modified</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (ViewBag.MeetingVenues != null)
                            {
                                @foreach (var venue in ViewBag.MeetingVenues as List<MOM_Project.Models.MeetingVenueModel>)
                                {
                                    <tr>
                                        <td>@venue.MeetingVenueID</td>
                                        <td>@venue.MeetingVenueName</td>
                                        <td>@venue.Created.ToString("yyyy-MM-dd HH:mm:ss")</td>
                                        <td>@venue.Modified.ToString("yyyy-MM-dd HH:mm:ss")</td>
                                        <td>
                                            <button class="btn btn-sm btn-primary">Edit</button>
                                            <button class="btn btn-sm btn-danger">Delete</button>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" class="text-center">No meeting venues found</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->
                </div>
            </div>
        </div>
    </div>
</section>